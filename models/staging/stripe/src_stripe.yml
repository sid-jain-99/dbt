version: 2

sources:
  - name: stripe
    database: raw  
    schema: stripe  
    tables:
      - name: payment
        config:
          loaded_at_field: _BATCHED_AT
          freshness: 
            error_after:
              count: 24
              period: day
            warn_after:
              count: 12
              period: hour
